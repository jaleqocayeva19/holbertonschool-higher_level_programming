The ``6-max_integer`` module
============================

Using ``max_integer``
---------------------

Importing the function:
    >>> max_integer = __import__('6-max_integer').max_integer

Testing an ordered list of integers:
    >>> max_integer([1, 2, 3, 4])
    4

Testing an unordered list of integers:
    >>> max_integer([1, 3, 4, 2])
    4

Testing a list where max value is at the beginning:
    >>> max_integer([4, 3, 2, 1])
    4

Testing an empty list (should return None):
    >>> max_integer([])

Testing a single element list:
    >>> max_integer([7])
    7

Testing a list of float numbers:
    >>> max_integer([1.53, 6.33, -1.2, 4.2])
    6.33

Testing a list with integers and floats:
    >>> max_integer([1.53, 14.5, 9, -2])
    14.5

Testing a list containing only negative numbers:
    >>> max_integer([-10, -5, -2, -50])
    -2

Testing a string input:
    >>> max_integer("Holberton")
    'r'

Testing a list of strings:
    >>> max_integer(["apple", "banana", "cherry"])
    'cherry'
